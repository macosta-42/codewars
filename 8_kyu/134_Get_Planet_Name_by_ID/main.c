#include <criterion/criterion.h>

const char * getPlanetName(int id);

Test(getPlanetName, should_pass_all_the_tests_provided)
{
// Create your own tests here. These are some of the functions available:
//     cr_assert(condition, [optional] format_string, [optional] format_arguments);
//     cr_assert_eq(actual, expected, [optional] format_string, [optional] format_arguments);
//     cr_assert_str_eq(actual, expected, [optional] format_string, [optional] format_arguments);
// see http://criterion.readthedocs.io/en/master/assert.html
}